server:
  port: 8080

spring:
  application:
    name: personal-blog-backend
  datasource:
    url: jdbc:mysql://47.107.88.43:3306/personal_blog?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Nfs_2i3-=Te,`?bd
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 47.107.88.43
      port: 6379
      password: Nfs_2i3-=Te,`?bd
      database: 0
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  file:
    name: logs/app.log
  level:
    com.blog.backend: DEBUG
    org.springframework: INFO

mybatis-plus:
  configuration:
    map-underline-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# JWT 配置
jwt:
  secret: zhangsan-lisi-wangwu-maliu-blog-secret-key-1234567890
  expiration: 86400 # 24小时

# Knife4j 配置
knife4j:
  enable: true
  setting:
    language: zh_cn
