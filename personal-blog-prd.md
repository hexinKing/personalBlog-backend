# 个人博客项目需求文档

**版本**: v1.0  
**日期**: 2026-05-15  
**依据**: 当前后端代码静态分析  
**范围**: 在现有个人博客后端能力基础上，补齐内容管理、互动、安全、运营与体验能力  

## 1. 项目概述

当前项目是一个基于 Spring Boot 3、Spring Security、JWT、MyBatis-Plus、MySQL、Redis 的个人博客后端系统。已有能力覆盖文章发布与浏览、分类标签、评论审核、用户认证、日志读取、统一响应和异常处理等基础功能。

下一阶段目标是将系统从“基础博客后端”升级为“内容创作 + 阅读发现 + 互动社区 + 后台运营”的个人内容平台。在保持轻量、易维护的前提下，重点增强安全性、内容组织能力、评论互动体验、后台管理效率和阅读分发能力。

## 2. 当前核心功能模块

### 2.1 认证与用户模块

当前能力：

- 用户登录，成功后返回 JWT token。
- 用户注册，支持用户名与邮箱唯一性检查。
- 密码使用 BCrypt 加密保存。
- 支持通过邮箱重置密码。
- 支持根据用户名查询用户信息，并隐藏密码字段。

涉及代码：

- `AuthController`
- `UserController`
- `UserServiceImpl`
- `JwtUtils`
- `JwtAuthenticationFilter`
- `SecurityConfig`

### 2.2 文章模块

当前能力：

- 文章分页列表。
- 按分类、标签、关键词、状态筛选文章。
- 文章详情查询。
- 新增或更新文章。
- 删除文章。
- 热门文章查询，按阅读量倒序取前 10 条。
- 热门文章结果使用 Redis 缓存 1 小时。
- 文章详情访问时增加阅读量，并使用 Redis 实现同一 IP 24 小时内只计一次。

涉及代码：

- `ArticleController`
- `ArticleServiceImpl`
- `Article`
- `ArticleVO`
- `ArticleQueryDTO`
- `ArticleTag`

### 2.3 分类模块

当前能力：

- 查询全部分类。
- 新增或更新分类。
- 删除分类。

涉及代码：

- `CategoryController`
- `CategoryServiceImpl`
- `Category`

### 2.4 标签模块

当前能力：

- 查询全部标签。
- 新增或更新标签。
- 删除标签。

涉及代码：

- `TagController`
- `TagServiceImpl`
- `Tag`

### 2.5 评论模块

当前能力：

- 查询指定文章下审核通过的评论。
- 提交评论，默认状态为审核中。
- 审核评论，支持修改评论状态。
- 评论实体中已有 `parentId`，具备扩展回复结构的基础。

涉及代码：

- `CommentController`
- `CommentServiceImpl`
- `Comment`

### 2.6 通用与运维辅助模块

当前能力：

- 统一响应结构 `Result<T>`。
- 全局异常处理。
- Knife4j / OpenAPI 文档能力。
- CORS 配置。
- 日志读取接口。

涉及代码：

- `Result`
- `GlobalExceptionHandler`
- `LogController`
- `application.yml`

## 3. 当前主要缺口

### 3.1 权限与安全缺口

- 当前仅区分“已登录”和“未登录”，没有管理员、作者、普通用户等角色模型。
- 后台接口缺少更细粒度的权限控制。
- 重置密码仅凭邮箱和新密码即可完成，缺少验证码、一次性 token 或身份校验。
- JWT 缺少刷新、退出登录、黑名单或失效控制。
- CORS 当前允许任意来源，在生产环境存在风险。
- 数据库、Redis、JWT secret 等敏感配置存在硬编码问题。

### 3.2 内容管理缺口

- 文章实体字段较基础，缺少封面图、作者、SEO 标题、SEO 描述、URL slug、阅读时长、置顶、推荐等内容运营字段。
- 新增或更新文章时，当前接口只保存文章主体，没有完整处理文章与标签的绑定关系。
- 删除文章时缺少关联数据处理策略，例如文章标签关系、评论、缓存。
- 缺少文章版本历史、预览、定时发布、归档等创作工作流。

### 3.3 评论互动缺口

- 评论列表当前是平铺结构，未利用 `parentId` 返回楼中楼或树形回复。
- 缺少后台评论列表，例如待审核、已通过、已拒绝筛选。
- 缺少评论删除、批量审核、敏感词过滤、频率限制。
- 缺少评论通知与回复提醒。

### 3.4 查询与阅读体验缺口

- 关键词搜索只覆盖标题和摘要，未覆盖正文全文。
- 热门文章只按阅读量排序，无法体现评论、发布时间、近期热度等综合因素。
- 热门文章缓存没有在文章更新、删除、阅读量变化时主动刷新。
- 缺少归档页、标签云、相关文章推荐、RSS、sitemap 等博客常见阅读入口。

### 3.5 工程质量缺口

- 虽然引入了 validation 依赖，但请求 DTO 缺少明确校验规则。
- 缺少单元测试、接口测试和关键业务回归测试。
- 缺少数据库迁移脚本。
- 缺少操作审计能力，难以追踪后台关键操作。

## 4. 产品目标

### 4.1 短期目标

- 补齐后台管理的安全边界。
- 完善文章、标签、分类、评论的管理闭环。
- 提升评论审核、文章发布、阅读浏览的基础体验。
- 消除明显的安全与配置风险。

### 4.2 中期目标

- 建立完整的内容创作工作流。
- 提供更强的内容发现能力，例如全文搜索、归档、标签云、相关推荐。
- 建立基础数据运营看板，帮助博主理解内容表现。

### 4.3 长期目标

- 将博客升级为个人知识与作品展示平台。
- 引入 AI 辅助创作、自动摘要、智能标签、SEO 建议等创新能力。
- 支持订阅、通知、收藏、点赞等轻社区能力。

## 5. 用户角色

### 5.1 访客

未登录用户，可浏览公开内容并参与轻量互动。

核心诉求：

- 快速找到感兴趣的文章。
- 阅读文章详情。
- 查看分类、标签、热门文章。
- 提交评论并等待审核。

### 5.2 登录用户

拥有账户的读者，可管理自己的互动记录。

核心诉求：

- 登录后维护个人资料。
- 管理自己的评论。
- 收藏、点赞或订阅内容。
- 接收评论回复通知。

### 5.3 作者

负责内容创作和发布。

核心诉求：

- 编写、预览、保存草稿、发布文章。
- 管理文章分类与标签。
- 查看文章阅读和互动数据。
- 优化文章 SEO 与内容分发。

### 5.4 管理员

拥有系统管理权限。

核心诉求：

- 管理用户、文章、分类、标签、评论。
- 审核评论。
- 查看操作日志和内容数据。
- 维护系统配置。

## 6. 功能需求

### 6.1 认证与权限

#### 6.1.1 角色权限

需求说明：

- 增加用户角色字段，至少支持 `ADMIN`、`AUTHOR`、`USER`。
- 后台管理接口仅允许 `ADMIN` 或 `AUTHOR` 访问。
- 系统管理、日志查看、用户管理仅允许 `ADMIN` 访问。
- 普通登录用户只能管理自己的资料和互动数据。

验收标准：

- 未登录用户无法访问后台写操作接口。
- 普通用户无法访问文章管理、评论审核、日志查看接口。
- 管理员可以访问全部后台管理接口。

#### 6.1.2 安全登录

需求说明：

- 登录失败次数达到阈值后进行短时间限制。
- JWT 支持刷新机制。
- 支持退出登录，使当前 token 失效。
- 用户密码修改后，旧 token 应失效或进入不可用状态。

验收标准：

- 连续登录失败后，接口返回明确限制提示。
- token 过期前可通过刷新接口换取新 token。
- 退出登录后，旧 token 无法继续访问受保护接口。

#### 6.1.3 安全重置密码

需求说明：

- 重置密码必须经过邮箱验证码或一次性 token 校验。
- 验证码应有有效期和错误次数限制。
- 新密码需要满足复杂度要求。

验收标准：

- 仅提供邮箱和新密码不能直接重置密码。
- 验证码过期或错误时，重置失败。
- 弱密码无法通过校验。

### 6.2 文章管理

#### 6.2.1 文章字段增强

需求说明：

文章应扩展以下字段：

- 封面图。
- 作者 ID。
- URL slug。
- SEO 标题。
- SEO 描述。
- 阅读时长。
- 是否置顶。
- 是否推荐。
- 发布时间。
- 归档状态。

验收标准：

- 文章列表可展示封面图、摘要、分类、标签、阅读量、发布时间。
- 文章详情可展示 SEO 信息、作者信息、标签和相关推荐。
- slug 唯一，且可用于前台详情页路由。

#### 6.2.2 文章发布工作流

需求说明：

- 支持草稿、已发布、已下线、已归档状态。
- 支持保存草稿。
- 支持预览文章。
- 支持定时发布。
- 支持文章下线。
- 支持文章版本历史。

验收标准：

- 草稿文章不会出现在前台公开列表。
- 定时发布文章到达发布时间后自动可见。
- 每次重要修改可记录历史版本。
- 作者可以查看并恢复历史版本。

#### 6.2.3 文章标签关系管理

需求说明：

- 创建或更新文章时，可同时提交标签 ID 列表。
- 后端应同步维护 `article_tag` 关系。
- 删除文章时应清理关联的文章标签关系。

验收标准：

- 文章保存后，列表和详情中的标签展示正确。
- 修改文章标签后，旧标签关系被正确更新。
- 删除文章后，不遗留无效文章标签关系。

#### 6.2.4 热门与推荐文章

需求说明：

- 热门文章不再只按阅读量排序。
- 综合阅读量、评论数、发布时间、是否推荐计算热度。
- 支持管理员手动推荐文章。
- 文章更新、删除、状态变化后，应主动刷新或失效相关缓存。

验收标准：

- 热门文章能反映近期内容热度。
- 已下线或草稿文章不会进入热门列表。
- 推荐文章可由后台配置。

### 6.3 分类与标签

#### 6.3.1 分类增强

需求说明：

- 分类支持排序。
- 分类支持别名 slug。
- 分类支持启用/停用状态。
- 分类列表展示文章数量。

验收标准：

- 前台分类按后台配置顺序展示。
- 停用分类不在前台展示。
- 分类页能展示该分类下文章数量和文章列表。

#### 6.3.2 标签增强

需求说明：

- 标签支持颜色、描述、排序。
- 标签列表展示文章数量。
- 支持标签云。
- 删除标签前应提示关联文章数量。

验收标准：

- 前台可展示标签云。
- 标签详情页可展示该标签下的文章列表。
- 删除有关联文章的标签时，有明确处理策略。

### 6.4 评论与互动

#### 6.4.1 树形评论

需求说明：

- 基于 `parentId` 支持评论回复。
- 文章详情评论区返回树形结构。
- 支持按时间正序或倒序展示。

验收标准：

- 用户可回复指定评论。
- 前台能展示评论与回复层级。
- 仅审核通过的评论在前台可见。

#### 6.4.2 评论审核后台

需求说明：

- 支持按状态查询评论：待审核、已通过、已拒绝。
- 支持单条审核。
- 支持批量审核。
- 支持删除评论。
- 支持按文章、昵称、邮箱、关键词筛选评论。

验收标准：

- 管理员可快速处理待审核评论。
- 审核通过后评论出现在前台。
- 拒绝或删除的评论不会在前台展示。

#### 6.4.3 评论风控

需求说明：

- 增加评论频率限制。
- 增加敏感词过滤。
- 增加邮箱格式校验。
- 可选增加图形验证码或人机校验。

验收标准：

- 短时间重复提交评论会被限制。
- 含敏感词评论进入审核或直接拒绝。
- 非法邮箱格式无法提交。

### 6.5 搜索与内容发现

#### 6.5.1 全文搜索

需求说明：

- 搜索范围从标题和摘要扩展到正文内容。
- 支持关键词高亮。
- 支持按分类、标签、时间、热度筛选。

验收标准：

- 搜索正文关键词可以命中文章。
- 搜索结果展示标题、摘要、高亮片段、分类、标签。

#### 6.5.2 归档与聚合页

需求说明：

- 支持按年份、月份归档文章。
- 支持分类聚合页。
- 支持标签聚合页。
- 支持最新文章、热门文章、推荐文章模块。

验收标准：

- 前台可按时间线浏览历史文章。
- 分类和标签页能稳定展示对应文章。

#### 6.5.3 RSS 与 sitemap

需求说明：

- 提供 RSS 订阅。
- 生成 sitemap，便于搜索引擎收录。
- 文章发布、下线后同步更新 sitemap。

验收标准：

- RSS 地址可被订阅工具识别。
- sitemap 中只包含公开可访问文章。

### 6.6 后台运营

#### 6.6.1 数据看板

需求说明：

- 展示文章总数、已发布文章数、草稿数。
- 展示总阅读量、近期阅读趋势。
- 展示评论总数、待审核评论数。
- 展示热门文章排行。
- 展示分类和标签内容分布。

验收标准：

- 管理员进入后台后可以快速了解博客内容表现。
- 数据统计口径清晰，且不会包含草稿或已下线文章的公开阅读数据。

#### 6.6.2 操作审计

需求说明：

- 记录关键后台操作，例如登录、发布文章、删除文章、审核评论、修改分类标签。
- 审计日志包含操作人、操作类型、目标对象、操作时间、IP。
- 支持按操作类型、操作人、时间范围查询。

验收标准：

- 管理员可以追踪关键内容变更。
- 出现误操作时，可以定位操作来源。

#### 6.6.3 日志查看增强

需求说明：

- 日志查看接口仅管理员可访问。
- 支持按行数读取。
- 支持关键词过滤。
- 支持按日志级别筛选。

验收标准：

- 非管理员无法访问日志接口。
- 管理员可以快速定位错误日志。

### 6.7 媒体资源管理

需求说明：

- 支持上传文章封面图和正文图片。
- 支持图片大小、格式校验。
- 支持图片压缩。
- 支持媒体资源列表。
- 可扩展接入对象存储，例如 OSS、S3、MinIO。

验收标准：

- 作者可以上传并选择文章封面图。
- 非图片或超大文件无法上传。
- 删除未使用图片时有明确提示。

### 6.8 AI 创作辅助

需求说明：

- 根据文章正文自动生成摘要。
- 根据文章内容推荐标签。
- 根据文章内容生成 SEO 标题和 SEO 描述。
- 根据文章内容生成社交平台分享文案。
- 可选支持错别字、语气、标题吸引力优化建议。

验收标准：

- 作者可在编辑文章时一键生成摘要和标签建议。
- 生成内容可编辑，不直接强制覆盖原文。
- AI 生成动作应记录操作来源，便于追踪。

## 7. 非功能需求

### 7.1 安全性

- 敏感配置必须通过环境变量或配置中心注入。
- 生产环境禁止将数据库、Redis、JWT 密钥硬编码在仓库配置中。
- 后台写操作必须经过权限校验。
- 用户输入必须进行参数校验。

### 7.2 性能

- 文章列表、热门文章、分类标签聚合数据可使用缓存。
- 缓存应设计主动失效策略。
- 文章列表应避免明显的 N+1 查询问题。
- 大量评论场景下应支持分页或折叠加载。

### 7.3 可维护性

- 建议引入数据库迁移工具管理表结构变更。
- 关键业务逻辑需要单元测试或接口测试覆盖。
- 请求对象与实体对象应逐步解耦，使用 DTO 承载接口入参。
- 错误码和错误信息应统一管理。

### 7.4 可观测性

- 关键业务操作应记录结构化日志。
- 异常日志应避免泄露敏感信息。
- 审计日志与系统日志应区分用途。

## 8. 优先级规划

### P0：必须优先完成

- 增加角色权限模型。
- 重置密码安全改造。
- 文章保存时维护标签关系。
- 评论审核后台列表。
- 请求参数校验。
- 敏感配置脱敏。
- 日志接口权限收紧。

### P1：重要增强

- 文章字段扩展。
- 草稿、发布、下线、归档工作流。
- 树形评论。
- 全文搜索。
- 热门文章综合排序。
- 热门缓存主动失效。
- 分类标签文章数量统计。
- 数据看板。

### P2：体验与创新

- 文章版本历史。
- 定时发布。
- RSS 与 sitemap。
- 媒体资源管理。
- AI 摘要、标签、SEO 建议。
- 订阅通知、点赞、收藏。

## 9. 推荐迭代路线

### 第一阶段：安全与后台闭环

目标：

- 让后台管理具备可上线的安全边界。
- 让文章、评论、标签、分类形成完整管理闭环。

范围：

- 角色权限。
- 安全重置密码。
- 文章标签关系维护。
- 评论审核列表。
- 输入校验。
- 配置脱敏。

### 第二阶段：内容发现与阅读体验

目标：

- 提升访客找内容、读内容、继续读内容的体验。

范围：

- 全文搜索。
- 归档页。
- 标签云。
- 相关文章。
- 热门和推荐文章优化。
- RSS 与 sitemap。

### 第三阶段：创作效率与运营能力

目标：

- 提升博主创作效率和内容运营效率。

范围：

- 数据看板。
- 操作审计。
- 文章版本历史。
- 定时发布。
- 媒体库。
- AI 创作辅助。

## 10. 页面与后台菜单建议

### 前台页面

- 首页文章流。
- 文章详情页。
- 分类页。
- 标签页。
- 归档页。
- 搜索结果页。
- 关于页。

### 后台页面

- 登录页。
- 数据看板。
- 文章列表。
- 文章编辑器。
- 分类管理。
- 标签管理。
- 评论审核。
- 用户管理。
- 媒体资源管理。
- 操作审计。
- 日志查看。

## 11. 关键验收标准

- 访客只能访问公开内容和提交评论。
- 管理员可以管理文章、分类、标签、评论、用户、日志。
- 作者可以创建、编辑、预览、发布、下线自己的文章。
- 创建或更新文章时，标签关系正确保存。
- 文章列表和详情展示分类、标签、阅读量、发布时间等信息。
- 评论提交后默认进入审核，审核通过后前台可见。
- 评论支持回复结构。
- 重置密码必须经过验证码或一次性 token。
- 敏感配置不再出现在仓库配置文件中。
- 热门文章不会展示草稿、下线或归档内容。
- 关键后台操作可以被审计追踪。

